class Boolean {

    main {

        boolean a = true;
        boolean b = false;

        boolean c = !(a || b);
        boolean d = !(b || a);
        boolean e = !(a || a);
        boolean f = !(b || b);

    }

}