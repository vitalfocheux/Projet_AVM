class fct{

    int sum(int a, int b, int c){
        if (a > 0) {
            a = a;
        } else {
            b = b;
        };
        return a + b + c;
    };

    main{
        int a = 1;
        int b = 2;
        int c = 3;
        int d = sum(a, b, c);
        write("sum = ");
        writeln(d);
    }
}