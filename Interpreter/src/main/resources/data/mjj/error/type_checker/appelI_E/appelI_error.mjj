class C {
  int x = 0;
  int y = 10;
  boolean b1 = false;
  boolean b2 = true;

   void f(int z){
      x = y * z;
      x = x / z;
      x++;
   };

   main {
 	if  (b1 || (!b2)) {
	f(); 
   var();
   f(b1);
   x = f(2);
   };
   
}
}
